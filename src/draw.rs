// use crate::common::*;
//
// pub fn draw_map(polish_tokens : Vec<String>) {
//     let mut stack = Vec::new();
//
//     for token in polish_tokens {
//         if check_str_number(token) {
//             stack.push(str_to_num(token.to_string()));
//         }
//     }
// }
//
// fn str_to_num(string : String) -> isize {
//     let number = 0;
//
//     number
// }
