use crate::common::*;

pub fn transfer_to_polish(tokens : Vec<String>, polish_tokens : &mut Vec<String>) -> bool {
    let exit_str = String::new();
    let mut stack : Stack<String>;

    for elem in tokens {
        if check_str_number(elem.clone()) {

        }
    }

    true
}
